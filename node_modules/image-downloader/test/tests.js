'use strict';

// var should = require('should');
var image_downloader = require('../index');

image_downloader({
    url: 'http://aprendizorganolo.webcindario.com/imagenes/ico_wikipedia.png',
    dest: '/tmp', 
    done: function(err, filename, image) {
        if (err) {
            throw err;
        }
        console.log('File saved to', filename);
    },
});

image_downloader({
    url: 'http://aprendizorganolo.webcindario.com/imagenes/ico_wikipedia.png',
    dest: '/tmp/image.png', 
    done: function(err, filename, image) {
        if (err) {
            throw err;
        }
        console.log('File saved to', filename);
    },
});
